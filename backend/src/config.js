import { config as dotenv } from "dotenv";
dotenv();

export const config = {
  host: "bjgiiot.tk",
  user: "admin_IoT",
  password: "Liam2020&",
  database: "admin_app",
  port: 3306,
  multipleStatements: true,
};
